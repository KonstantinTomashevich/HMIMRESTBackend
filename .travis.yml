language: java
script: mvn package
deploy:
  provider: script
  skip_cleanup: true
  script: heroku auth:token &&
          heroku container:login --app ${HEROKU_APP} &&
          heroku container:push web --app ${HEROKU_APP} &&
          heroku container:release web --app ${HEROKU_APP}
  on:
    branch: master